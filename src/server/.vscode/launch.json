{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "start-server",
            "request": "launch",
            "type": "node",
            "program": "${workspaceFolder}/bin/server"
        },
    ]
}